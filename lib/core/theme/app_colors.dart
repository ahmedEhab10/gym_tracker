import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xff1A1A1A);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color card = Color(0xFF252525);

  // Primary & Accents
  static const Color primary = Color(0xFF7BFF00); // Neon Green
  static const Color accent = Color(0xFFFF0000); // Red
  static const Color secondary = Color(0xFF1E88E5); // Blue (Optional)

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB3B3B3);
  static const Color textHint = Color(0xFF666666);

  // States
  static const Color error = Color(0xFFCF6679);
  static const Color success = Color(0xFF00FF41);
  static const Color warning = Color(0xFFFFC107);
}
